# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080

# Database Provider (SqlServer or PostgreSQL)
DATABASE_PROVIDER=SqlServer

# Database Connection Strings
# Uncomment the one you're using:

# SQL Server / Azure SQL Edge
ConnectionStrings__BlazorWebAppMoviesContext=Server=your-sql-server-host,1433;Database=BlazorWebAppMovies;User Id=your-username;Password=your-password;TrustServerCertificate=True;MultipleActiveResultSets=true

# PostgreSQL
# ConnectionStrings__BlazorWebAppMoviesContext=Host=your-postgres-host;Database=BlazorWebAppMovies;Username=your-username;Password=your-password

# Logging Configuration
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft_AspNetCore=Warning

# Allowed Hosts (comma-separated list)
AllowedHosts=*

# Optional: HTTPS Configuration (if using HTTPS)
# ASPNETCORE_HTTPS_PORTS=8081
# ASPNETCORE_Kestrel__Certificates__Default__Password=your-cert-password
# ASPNETCORE_Kestrel__Certificates__Default__Path=/path/to/cert.pfx
